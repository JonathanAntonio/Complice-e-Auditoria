# Notification Service

Microserviço para despacho e rastreamento de notificações (email/webhook).

- **Porta HTTP:** 3008 (ou `NOTIFICATION_SERVICE_PORT`)
- **Rotas:** `/api/v1/notifications/dispatch`, `/api/v1/notifications/logs`, `/health`, `/api-docs`
