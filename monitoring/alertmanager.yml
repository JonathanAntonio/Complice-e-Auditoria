route:
  receiver: default
  group_by: [alertname, service, job]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs:
      - url: "http://host.docker.internal:9999/alerts"
        send_resolved: true
