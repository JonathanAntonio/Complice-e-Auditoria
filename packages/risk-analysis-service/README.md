# Risk Analysis Service

Microserviço para cálculo e consulta de pontuações de risco por usuário, área e processo.

- **Porta HTTP:** 3006 (ou `RISK_SERVICE_PORT`)
- **Rotas:** `/api/v1/risk/scores`, `/api/v1/risk/events`, `/health`, `/api-docs`
